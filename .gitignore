# Python build artefacts
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Office lock files (Word / Excel / PowerPoint)
~$*

# Editor
.vscode/
.idea/
*.swp
extracted-books/
skills/_extraction-cache/

# Project workspaces — kept local, never pushed.
# Engine code, skills, docs/plans, scripts, etc. are pushed as the shared engine.
# Per-project research, drafts, evidence, and outputs live under projects/<id>/
# and stay on the operator's machine.
projects/
