# AGENTS.md - 00-meta-initialization

Use this skill immediately after `python -m engine new-project`. Preserve the
hard anti-hallucination clause from `source-evaluation` in any delegated
research prompt.
