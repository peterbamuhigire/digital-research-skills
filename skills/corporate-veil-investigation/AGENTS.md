# Codex / generic-agent — corporate-veil-investigation

```yaml
trace:
  seed: {entity_id, name}
  max_depth_up: 5
  max_depth_down: 3
  registries: [companies_house, edgar, sos, opencorporates]
graph:
  nodes: [...]
  edges: [...]
  shared_officers: [...]
  shared_addresses: [...]
shell_flags: [...]
mermaid_render: "graph TD ..."
```

See `SKILL.md`.
