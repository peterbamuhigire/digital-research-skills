# Claude-specific — corporate-veil-investigation

- Always walk both parent (up) and subsidiaries (down).
- Cross-check every entity in the graph against `sanctions-pep-screening` + `adverse-media-investigation`.
- Render the graph as Mermaid for inclusion in the DD report.
- Flag shell-haven registrations explicitly.

See `SKILL.md`.
