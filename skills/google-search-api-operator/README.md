# google-search-api-operator

Programmatic Google search via Custom Search JSON API + SerpAPI + Brave. Closes the biggest gap in Brown's *Harnessing the Power of Google*. Backed by `tools/google/search_api.py`.

See `SKILL.md`.
