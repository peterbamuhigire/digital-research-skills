# Claude-specific — evidence-custody-discipline

- Use `tools.pi.ChainOfCustody` for any evidentiary item.
- Append-only — never edit prior entries.
- Run `verify_integrity()` before producing the affidavit for court.
- Mark seals with case#, date, time, name, location, signature, witnesses.

See `SKILL.md`.
