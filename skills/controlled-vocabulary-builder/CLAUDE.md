# Claude-specific — controlled-vocabulary-builder

- File at `projects/<project-id>/VOCABULARY.md`.
- Build incrementally — add as concepts emerge from reading and minianalyses.
- When the domain has its own controlled vocabulary (MeSH, ERIC, LCSH), use it as the canonical layer.

See `SKILL.md`.
