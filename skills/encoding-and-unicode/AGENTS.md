# Codex / generic-agent — encoding-and-unicode

```yaml
repair:
  detected_encoding: "..."
  confidence: 0.0-1.0
  bom_stripped: bool
  mojibake_fixes: <int>
  final_encoding: utf-8
  backup_path: "...bak"
```

See `SKILL.md`.
