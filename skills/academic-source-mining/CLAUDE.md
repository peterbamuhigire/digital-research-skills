# Claude-specific guidance — academic-source-mining

- Brief sub-agents to **specifically search university dissertation repositories** — they default to journalism otherwise.
- Use `WebFetch` to retrieve abstracts; never cite a paper from its title alone.
- Group `<cohort>/research/academic-publications.md` by source type (peer-reviewed / dissertation / working paper).

See `SKILL.md`.
