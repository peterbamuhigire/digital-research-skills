# dataset-discovery-and-analysis

Find / retrieve / profile public datasets as a first-class research source. Backed by `tools/datasets/` (registry → search → retrieve → analyse). Includes Segnini's five-step verification (completeness / duplicates / accuracy / integrity / codes).

See `SKILL.md`.
