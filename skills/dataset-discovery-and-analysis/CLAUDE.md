# Claude-specific — dataset-discovery-and-analysis

- Use `tools/datasets/` not raw HTTP for dataset work.
- Always profile (`profile_dataset`) before analysing.
- Record SHA-256 + license + access date in `<cohort>/research/sources.md`.

See `SKILL.md`.
