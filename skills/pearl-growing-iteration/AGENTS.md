# Codex / generic-agent — pearl-growing-iteration

Per-pass output:

```yaml
pass: 1 | 2 | 3
recall_precision_target: max_recall | balanced | max_precision | fragment
query_used: "..."
results_count: <int>
harvested_vocab: [...]
top_authors: [...]
top_journals: [...]
top_orgs: [...]
next_pass_query: "..."
```

See `SKILL.md`.
