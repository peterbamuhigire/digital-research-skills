# Claude-specific — pearl-growing-iteration

- Default to two passes. Brief sub-agents to run a broad pass and report harvested vocabulary before the targeted pass.
- Use refine-panel snapshots (top authors / orgs / years) before reading individual hits.
- Label each query with its recall/precision target.

See `SKILL.md`.
