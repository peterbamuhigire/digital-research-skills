# Claude-specific — crosswalk-matrix

- File at `projects/<project-id>/CROSSWALK.md`. Update whenever a minianalysis lands.
- Mark `NR` (not relevant) cells eagerly — saves sub-agent time.
- Sub-agent dispatch picks `gap` cells, never re-runs `MA-NNN` cells without reason.

See `SKILL.md`.
