# Claude-specific — minianalysis-engine

- Always run an establishment minianalysis before deeper work.
- Cap each minianalysis at one full-time week (or sub-agent equivalent).
- File each as `projects/<project-id>/minianalyses/<id>-<short-title>.md`.
- Every minianalysis must produce 1–3 written paragraphs with inline citations.

See `SKILL.md`.
