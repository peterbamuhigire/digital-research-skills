# web-scraping-foundations

Decision tree (API → JSON XHR → HTML → headless), parser choice, structured-data shortcuts (JSON-LD/OG/sitemap/RSS), error taxonomy. Backed by `tools/scraping/`. From Lawson, Brody, Oxylabs, Hands-On.

See `SKILL.md`.
