# Claude-specific — web-scraping-foundations

- Use `tools.scraping` rather than ad-hoc requests.
- Always check JSON-LD before parsing the DOM.
- View Source + DevTools Network XHR before reaching for headless.
- Cache raw HTML — re-extraction is free.

See `SKILL.md`.
