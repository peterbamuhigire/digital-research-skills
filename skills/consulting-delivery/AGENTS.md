# Codex / generic-agent — consulting-delivery

```yaml
consulting_delivery:
  client_decision: "..."
  owner: "..."
  time_horizon: "..."
  issue_tree: required
  hypothesis_set: required
  workplan: required
  implementation_path: required
```

Never let the workplan outrun the problem definition.
