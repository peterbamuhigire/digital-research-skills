# Claude-specific guidance — regulatory-landscape-mapping

- Use `WebFetch` on Kenyalaw.org / ULII / TanzLII for case law — never paraphrase from journalism.
- Brief regulatory-research sub-agents specifically on the five-layer structure; otherwise they default to layer-2-only outputs.
- Always include the enforcement-gap section at the end of each country's mapping.

See `SKILL.md`.
