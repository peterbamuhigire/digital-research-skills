# originality-engine

Deterministic per-run variation knobs (hook / order / sentence length / verbs / paragraph pattern / header style). Same project + run → same knobs; different run → guaranteed different knobs. Backed by `tools/academic/variation.py`.

See `SKILL.md`.
