# Codex / generic-agent — google-stakeholder-recon

```yaml
topic: "..."
country_focus: [KE, UG, ...]
include_us_states: bool
include_igos: bool
extra_ngos: [...]
extra_academic: [...]
file_types: [pdf, xlsx, csv, docx]
expected_query_count: <int>     # ≈ stakeholders × file_types
```

See `SKILL.md`.
