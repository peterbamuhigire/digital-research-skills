# Claude-specific — google-stakeholder-recon

- Run before any general Google search on a domain you don't know well.
- Always include IGOs for cross-border topics.
- For US state research, search both legacy AND modern state domains.
- Iterate file types: pdf → xlsx → csv → docx.

See `SKILL.md`.
