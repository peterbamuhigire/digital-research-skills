# anomaly-detection

IQR + z-score + Cook's + KNN + Isolation Forest + STL residual panel. Skew-aware method selection. Walker's discipline: distinguish implausible from unusual. Backed by `tools/data/outlier_panel.py`.

See `SKILL.md`.
