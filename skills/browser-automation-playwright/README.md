# browser-automation-playwright

Playwright-first headless. Auto-waits, browser contexts, storage_state, network interception, codegen. Last resort after API → XHR → HTML. Backed by `tools/scraping/headless.py`.

See `SKILL.md`.
