# Claude-specific guidance — osint-methodology

- Use `WebFetch` for crt.sh, Wayback, archive.today.
- Note any tool that leaves a log (Shodan, Censys queries are logged).
- Never use leaked data — even if technically accessible.
- Always include a "verification limits" section.

See `SKILL.md`.
