# web-search-operator-grammar

Operator stack (punctuation × field × refinement × Boolean) as the lowest layer of web search craft. Adapted from Amanda Deason, *Internet Research with Google*.

See `SKILL.md` for the canonical reference + per-engine syntax matrix.
