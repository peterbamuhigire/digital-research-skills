# Claude-specific guidance — web-search-operator-grammar

- Apply this skill before issuing any `WebFetch` or web-search tool call.
- Phrase-quote multi-word concepts by default.
- Combine `site:.gov` / `site:.edu` + `filetype:pdf` for institutional grey lit.
- For "broaden when sparse" — drop terms one at a time, not all at once.

See `SKILL.md`.
