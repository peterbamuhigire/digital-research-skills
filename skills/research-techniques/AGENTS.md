# Codex / generic-agent — research-techniques

```yaml
research_technique:
  name: minianalysis | crosswalk | cross_cohort | gap_analysis | reference_interview | controlled_vocabulary | pain_point_taxonomy | search_operator | google_search_api
  artifact:
    type: list | table | taxonomy | query_string | reframed_query
    fields: [...]
  inputs: [...]
  outputs: [...]
  pairs_with: [research-orchestration, source-evaluation]
```

See `SKILL.md`.
