# Claude-specific — research-techniques

- Load only the matching reference per technique; do not load the full library.
- Every technique produces a structured artifact the orchestrator can consume — never a free-text summary.
- Pain-point taxonomies are built from coded raw data only; never invented.
- Gap analysis names what the literature has not answered, not what you have not read yet.
- Cross-cohort synthesis runs after each cohort has its own complete corpus, never before.
- Use search-operator grammar verbatim in queries; do not paraphrase operators.

See `SKILL.md`.
