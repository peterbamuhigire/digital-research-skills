# Codex / generic-agent guidance — trend-analysis

Per-trend output:

```yaml
trend: "..."
signal_type: coincident | lagging | leading | durable | fad
drivers: [...]
symptoms: [...]
scenarios:
  best: {assumption, outcome}
  base: {assumption, outcome}
  worst: {assumption, outcome}
leading_indicators: [...]
confidence: high | medium | low
```

See `SKILL.md`.
