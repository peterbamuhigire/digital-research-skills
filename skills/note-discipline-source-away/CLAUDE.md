# Claude-specific — note-discipline-source-away

- After note extraction, drop the source text from context. Composition reads cards only.
- Card `point` is a FRAGMENT, never a sentence. >25 words = warning.
- Every paraphrase card carries a personal comment — the seed of original argument.
- Quote cards need `is_quote=True` + `source_page`.

See `SKILL.md`.
