# Claude-specific — tidy-data-craft

- Always tidy_check after retrieval, before analysis.
- Tidy = melt; wide = pivot. Pick one form per dataset.
- `pd.wide_to_long` for `Q1_sales / Q2_sales` pattern.

See `SKILL.md`.
