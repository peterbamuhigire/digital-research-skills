# scraping-politeness-and-ratelimiting

robots.txt + per-host throttling + exponential backoff + identification headers + block-detection signals. Default rules apply on every scrape. Backed by `tools/scraping/`.

See `SKILL.md`.
