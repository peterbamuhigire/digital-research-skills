# Claude-specific guidance — cross-cohort-synthesis

- Run after all cohort `analysis/themes.md` files are complete.
- Read all cohort themes files into context, then build the synthesis matrix in a single pass.
- Never delegate this to a sub-agent — context completeness is the whole point.

See `SKILL.md`.
