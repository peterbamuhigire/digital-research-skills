# Codex / generic-agent guidance — social-source-extraction

For tool-having Codex agents:

- Prefer PRAW + OAuth for Reddit
- Apify Reddit Scraper for high-volume runs
- Always tag corpus with platform + date + method
- Never write social-only claims to a final report — corroborate with higher-tier source

See `SKILL.md`.
