# Digital Research Kaizen — 2026-09-06

## Scope and baseline

Local engine and evidence-gate maintenance in C:/wamp64/www/digital-research-skills; no research project, private source collection or publication. The native gate passed 59 contracts, 29 routing fixtures, doctor and 22 kernel tests. The broader suite initially had 31 passes and one failure from hardcoded research/political checkout paths.

## Experiments and acceptance

The source-currency validator accepted empty catalogues, duplicate/coerced IDs, future access/verification dates and currentness claims without cited sources; malformed roots could crash. It now validates typed collections, unique nonblank IDs, date boundaries and claim references. Negative cases use temporary files. The CLI reports metadata/date assurance only, with claim support NOT ASSESSED. See [source-currency review](2026-09-06-kaizen-source-currency-review.md).

The machine-error gate now supports explicit target selection while requiring all registered targets by default; empty and unknown selections fail. Corrected the canonical research path. Deterministic local tests no longer require siblings; installed-adapter tests require explicit opt-in. The confirmed 11-target integration run passed all identifier-presence checks. This does not certify semantic editorial quality. Visual adapter tests are a separately scoped integration check; see [review and disposition](2026-09-06-kaizen-machine-gate-review.md).

Integrated run: 115 tests and 11 subtests passed with SKILL_ENGINE_LIVE_TESTS=1 and the explicit eleven labels. Source-ingestion and diff gates passed. Canonical scanner: 58/59; the dissertation degraded-mode section explicitly returns a qualified plan with NOT ASSESSED gates, but the scanner's phrase heuristic misses it. Do not add boilerplate to manipulate that result.

## Output evidence matrix

| Product family | Evidence inspected | Unassessed outcome |
|---|---|---|
| Reports, due diligence, academic outputs | Routes, local contracts, source/claim metadata checks | Actual claim support, independent verification and professional review |
| Source registers and evidence packs | Malformed/duplicate/date/unknown-source regressions | Authenticity, source independence, citation entailment |
| Datasets and dashboards | Engine routes and output contracts | Dataset correctness, reproducible analysis and rendered accessibility |
| Proposals and Word/PDF outputs | Handoff routes and local tests | Client acceptance and full render/reopen fidelity |

## Next cycle

P0 closed: source metadata false passes and workstation-bound tests. P1: verify portfolio claim-register content and scope with primary sources, not only dates. P2: retain a full research-to-claim-to-render evidence chain and independent replication. Owner Peter Bamuhigire; review 2026-09-13. Target 95/100; overall quality raw/published score NOT ASSESSED and any numeric published score capped at 65.

Rollback only reviewed hunks; preserve source dates, records and independent findings. Source basis is context-bound local code/test evidence observed 2026-09-06, final verification on 2026-09-07. No current source was re-certified by changing metadata.

## Resumption follow-up: 2026-09-07

The portfolio source register exposed a contract mismatch: the owning currentness
guide allowed `context-bound` but the validator rejected it. The validator now
accepts that class only with a nonblank scope and valid review dates. Nine
regressions cover bounded scope, invalid scope types and overdue evidence.
The full suite passed 124 tests and 11 subtests; 22 kernel tests passed separately.
Evidence and the single freshly verified source row are recorded in the
engineering engine's `docs/audits/2026-09-06-kaizen/resume-2026-09-07/` pack.
Other source rows were not recertified.

Every future Kaizen now explicitly requires a latest-model and active-runtime
review. The engine's `.codex` adapter carries Peter's Codex-only model policy;
Claude keeps its own model choices and full domain access.
